# ── Gradle / build output ──
build/
.gradle/
*.class
*.jar.lock

# ── IDE / OS ──
.idea/
*.iml
.vscode/
.DS_Store
Thumbs.db

# ── Runtime / data (never commit live server data) ──
logs/
plugins/
*.log
sessions.yml
learning_dataset.yml
admin-actions.yml
updates-notified.yml

# ── Secrets (never commit) ──
*.key
*.pem
.env
credentials.json
*.token

# ── Ephemeral / tmp ──
/tmp/
*.tmp
*.bak
